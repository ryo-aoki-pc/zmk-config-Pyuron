if SHIELD_PYURON_L || SHIELD_PYURON_R
    config ZMK_KEYBOARD_NAME
        default "Pyuron"

    config NFCT_PINS_AS_GPIOS
        default y

    config ZMK_SPLIT
        default y

    config ZMK_POINTING
        default y

    config PAW3222
        default y

    config INPUT
        default y

    config SPI
        default y
endif
